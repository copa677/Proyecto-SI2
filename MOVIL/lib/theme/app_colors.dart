import 'package:flutter/material.dart';

class AppColors {
  static const Color rojo = Color(0xFFF04438); // Estados negativos, badges "Ausente"
  // Nueva paleta
  static const Color azulPrincipal = Color(0xFF437AD1); // Encabezados, botones, links activos, íconos principales
  static const Color azulClaro = Color(0xFFEAF1FB);     // Fondos sutiles para avatares, tags, iconos secundarios
  static const Color verdePrincipal = Color(0xFF1EC45B); // Progresos, estados positivos, badges "Activo"
  static const Color verdeClaro = Color(0xFFEAFBEE);     // Fondo barra de progreso, badges suaves
  static const Color blanco = Color(0xFFFFFFFF);         // Fondo principal
  static const Color grisMuyClaro = Color(0xFFF5F6FA);   // Cards, inputs, contornos
  static const Color grisLineas = Color(0xFFE4E7EC);     // Líneas divisorias, bordes
  static const Color grisTextoSecundario = Color(0xFF757575); // Texto menos importante
  static const Color negroTexto = Color(0xFF1A1A1A);     // Texto principal
}
